name: finsight
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0

  # Supabase
  supabase_flutter: ^2.5.0

  # UI & Utilities
  cupertino_icons: ^1.0.8
  intl: ^0.18.1
  permission_handler: ^11.4.0
  image_picker: ^1.2.0
  image: ^4.5.4

  # Google ML Kit & Firebase
  google_mlkit_text_recognition: ^0.11.0
  firebase_core: ^3.0.0 # This is a dependency for firebase_messaging
  firebase_messaging: ^15.0.1 # <-- This is the new line

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true